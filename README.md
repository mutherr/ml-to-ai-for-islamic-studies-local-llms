# ml-to-ai-for-islamic-studies-local-llms
Example scripts for running LLMs locally for scoring documents

Requirements:

You'll need to install OLLama and the python `llm` package to run models locally. 
